/***********************  tringle area ******************/

/*function tringleValue(arm1, arm2, arm3) {
    let s = (arm1 + arm2 + arm3) / 2;

    let area = (s * (s - arm1) * (s - arm2) * (s - arm3)) ** (1 / 2);
    return area;
}

let getValue = tringleValue(10, 15, 20);
console.log(getValue);*/

/*********************** circle area******************/


/*function giveRadious(radius) {
    const pai = 3.1416;
    let areaOfCircle = pai * radius ** 2;
    return areaOfCircle
}
let getRadious = giveRadious(10);
console.log(getRadious);*/



/***********************  rectangular area******************/

/*function giveLengthWidth(length, width) {
    let areaOfRectangular = length * width;
    return areaOfRectangular;
}
let putValue = giveLengthWidth(20, 10);
console.log(putValue);
*/


/***********************  trapigiam area******************/
/*function giveValue(arm1, arm2, height) {

    let areaOfTrapigiam = (1 / 2) * (arm1 + arm2) * height;
    return areaOfTrapigiam;
}
let putValue = giveValue(10, 20, 30);
console.log(putValue);*/