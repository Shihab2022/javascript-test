// ami jodi poriman chear table bed croy kortay chai say sonka ta parameter ay diat diboo and pottak ar dam perChearwood ay diay dibo taholay amder sob calculation diay dibay


/*function woodCalculate(chearQuantity, tableQuantity, bedQuantity) {
    const perChearWood = 3;
    const perTableWood = 15;
    const perBedWood = 55;

    const chearWoodQuantity = perChearWood * chearQuantity;
    const tableWoodQuantity = perTableWood * tableQuantity;
    const bedWoodQuantity = perBedWood * bedQuantity;

    const totalWoodQuantity = chearWoodQuantity + tableWoodQuantity + bedWoodQuantity;
    return totalWoodQuantity;
}

const firstOption = woodCalculate(0, 0, 1);
console.log(firstOption);*/